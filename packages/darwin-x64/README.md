# @native-audio-node/darwin-x64

This package contains the pre-built native binary for `native-audio-node` on macOS Intel (x64).

**This package is automatically installed** as an optional dependency when you install `native-audio-node` on a compatible platform.

## Do not install directly

```bash
# Do this instead:
npm install native-audio-node
```

## Platform Support

- **OS:** macOS (darwin)
- **Architecture:** Intel (x64)
- **macOS Version:** 14.2+ (Sonoma or later)

## License

MIT
